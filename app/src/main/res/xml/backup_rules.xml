<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- The GitHub token is encrypted with a Keystore key that never leaves the device,
         so backing it up would only restore an undecryptable blob. -->
    <exclude domain="sharedpref" path="nutrition_secrets.xml" />
</full-backup-content>
